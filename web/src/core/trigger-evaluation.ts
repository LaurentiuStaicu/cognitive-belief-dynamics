import type {StructuredTriggerCondition} from './adaptive-plan-runtime';
import type {ImplementationPlan} from '../implementation-plan';
import type {IndicatorProjection} from './indicator-objects';
import type {ObservedOutcome} from './observed-outcome';

export type TriggerEvaluationStatus='TRIGGER_MET'|'TRIGGER_NOT_MET'|'NOT_EVALUABLE';
export type TriggerEvaluationReason='COMPARISON_TRUE'|'COMPARISON_FALSE'|'INDICATOR_MISMATCH'|'UNIT_MISMATCH'|'PRE_SNAPSHOT_PHENOMENON'|'INVALID_TIME'|'REFERENCE_VALIDATION_FAILED';
export type TriggerEvaluation={object_type:'TriggerEvaluation';persistence:'DERIVED_NOT_STORED';automation:'NO_AUTOMATIC_ACTIONS';implementation_plan_id:string;prospective_snapshot_id:string;observed_outcome_id:string;indicator_id:string;comparator:StructuredTriggerCondition['comparator'];threshold:number;unit:string;observed_value:number;phenomenon_time:string;result_time:string;status:TriggerEvaluationStatus;reason:TriggerEvaluationReason};
export type TriggerEvaluationProjection={object_type:'TriggerEvaluationProjection';source:'PERSISTED_OBSERVED_OUTCOME_ONLY';persistence:'DERIVED_NOT_STORED';automation:'NO_AUTOMATIC_ACTIONS';implementation_plan_id:string;prospective_snapshot_id:string;trigger:StructuredTriggerCondition;evaluations:TriggerEvaluation[]};

function frozenTrigger(plan:ImplementationPlan):StructuredTriggerCondition{const iff=plan.adaptive_plan.find(step=>step.phase==='IF');if(!iff?.trigger)throw new Error('ImplementationPlan has no frozen structured trigger');return iff.trigger;}
function compare(value:number,trigger:StructuredTriggerCondition):boolean{switch(trigger.comparator){case 'LT':return value<trigger.threshold;case 'LTE':return value<=trigger.threshold;case 'GTE':return value>=trigger.threshold;case 'GT':return value>trigger.threshold;}throw new Error('unsupported trigger comparator');}
function referencesValid(plan:ImplementationPlan,outcome:ObservedOutcome,indicators:IndicatorProjection):boolean{if(plan.object_type!=='ImplementationPlan')return false;if(outcome.case_id!==plan.case_id)return false;if(outcome.implementation_plan_id!==plan.id)return false;if(outcome.prospective_snapshot_id!==plan.prospective_snapshot.id)return false;if(!plan.indicator_ids.includes(outcome.indicator_id))return false;const definition=indicators.indicators.find(item=>item.definition.id===outcome.indicator_id)?.definition;if(!definition)return false;if(outcome.observed_property!==definition.observed_property)return false;if(outcome.outcome.stage!==definition.target_stage)return false;if(definition.unit&&outcome.result.unit!==definition.unit)return false;if(definition.target_stage==='PROXIMAL'&&!plan.action_canvas.proximal_result.indicator_ids.includes(outcome.indicator_id))return false;if(definition.target_stage==='INTERMEDIATE'&&!plan.action_canvas.intermediate_result.indicator_ids.includes(outcome.indicator_id))return false;if(definition.target_stage==='FINAL'&&!plan.action_canvas.final_outcome.indicator_ids.includes(outcome.indicator_id))return false;return true;}
function notEvaluable(plan:ImplementationPlan,outcome:ObservedOutcome,trigger:StructuredTriggerCondition,reason:Exclude<TriggerEvaluationReason,'COMPARISON_TRUE'|'COMPARISON_FALSE'>):TriggerEvaluation{return {object_type:'TriggerEvaluation',persistence:'DERIVED_NOT_STORED',automation:'NO_AUTOMATIC_ACTIONS',implementation_plan_id:plan.id,prospective_snapshot_id:plan.prospective_snapshot.id,observed_outcome_id:outcome.id,indicator_id:outcome.indicator_id,comparator:trigger.comparator,threshold:trigger.threshold,unit:trigger.unit,observed_value:outcome.result.value,phenomenon_time:outcome.phenomenon_time,result_time:outcome.result_time,status:'NOT_EVALUABLE',reason};}

export function evaluateObservedOutcomeAgainstFrozenTrigger(input:{plan:ImplementationPlan;outcome:ObservedOutcome;indicators:IndicatorProjection}):TriggerEvaluation{
 const {plan,outcome,indicators}=input;const trigger=frozenTrigger(plan);if(!referencesValid(plan,outcome,indicators))return notEvaluable(plan,outcome,trigger,'REFERENCE_VALIDATION_FAILED');if(outcome.indicator_id!==trigger.indicator_id)return notEvaluable(plan,outcome,trigger,'INDICATOR_MISMATCH');if(outcome.result.unit!==trigger.unit)return notEvaluable(plan,outcome,trigger,'UNIT_MISMATCH');const frozenAt=Date.parse(plan.prospective_snapshot.frozen_at);const phenomenon=Date.parse(outcome.phenomenon_time);const resultTime=Date.parse(outcome.result_time);if(!Number.isFinite(frozenAt)||!Number.isFinite(phenomenon)||!Number.isFinite(resultTime))return notEvaluable(plan,outcome,trigger,'INVALID_TIME');if(phenomenon<frozenAt)return notEvaluable(plan,outcome,trigger,'PRE_SNAPSHOT_PHENOMENON');const met=compare(outcome.result.value,trigger);return {object_type:'TriggerEvaluation',persistence:'DERIVED_NOT_STORED',automation:'NO_AUTOMATIC_ACTIONS',implementation_plan_id:plan.id,prospective_snapshot_id:plan.prospective_snapshot.id,observed_outcome_id:outcome.id,indicator_id:outcome.indicator_id,comparator:trigger.comparator,threshold:trigger.threshold,unit:trigger.unit,observed_value:outcome.result.value,phenomenon_time:outcome.phenomenon_time,result_time:outcome.result_time,status:met?'TRIGGER_MET':'TRIGGER_NOT_MET',reason:met?'COMPARISON_TRUE':'COMPARISON_FALSE'};
}

export function buildTriggerEvaluationProjection(input:{plan:ImplementationPlan;outcomes:ObservedOutcome[];indicators:IndicatorProjection}):TriggerEvaluationProjection{
 const trigger=frozenTrigger(input.plan);const outcomes=[...input.outcomes].filter(item=>item.object_type==='ObservedOutcome'&&item.implementation_plan_id===input.plan.id).sort((a,b)=>a.result_time.localeCompare(b.result_time)||a.id.localeCompare(b.id));return {object_type:'TriggerEvaluationProjection',source:'PERSISTED_OBSERVED_OUTCOME_ONLY',persistence:'DERIVED_NOT_STORED',automation:'NO_AUTOMATIC_ACTIONS',implementation_plan_id:input.plan.id,prospective_snapshot_id:input.plan.prospective_snapshot.id,trigger:{...trigger},evaluations:outcomes.map(outcome=>evaluateObservedOutcomeAgainstFrozenTrigger({plan:input.plan,outcome,indicators:input.indicators}))};
}
