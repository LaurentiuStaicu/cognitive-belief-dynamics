import {createServer} from 'node:http';
import {readFile} from 'node:fs/promises';
import {fileURLToPath} from 'node:url';
import path from 'node:path';
import assert from 'node:assert/strict';
import {chromium} from 'playwright';
const dist=fileURLToPath(new URL('../dist/',import.meta.url));const prefix='/cognitive-epistemic-model/';const server=createServer(async(req,res)=>{try{const pathname=new URL(req.url,'http://localhost').pathname;if(!pathname.startsWith(prefix)){res.writeHead(404).end();return;}const file=path.resolve(dist,decodeURIComponent(pathname.slice(prefix.length))||'index.html');if(!file.startsWith(dist)){res.writeHead(403).end();return;}const data=await readFile(file);res.setHeader('Content-Type',({'.html':'text/html','.js':'text/javascript','.css':'text/css','.json':'application/json','.svg':'image/svg+xml'})[path.extname(file)]||'application/octet-stream');res.end(data);}catch{res.writeHead(404).end();}});await new Promise(resolve=>server.listen(0,'127.0.0.1',resolve));const address=server.address();assert(address&&typeof address!=='string');const url=`http://127.0.0.1:${address.port}${prefix}`;
let browser;
try{browser=await chromium.launch({headless:true,...(process.env.CEM_BROWSER_PATH?{executablePath:process.env.CEM_BROWSER_PATH,args:['--no-sandbox','--disable-gpu']}:{})});const page=await browser.newPage({viewport:{width:1440,height:1050},reducedMotion:'reduce'});const errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('response',r=>{if(r.status()>=400)errors.push(`${r.status()} ${r.url()}`);});
 const settle=async()=>{await page.locator('[data-suite-standard="InfoClar Model Suite Design Standard v1.1"]').waitFor();const theory=page.locator('#suiteTheoryContext #theoryArticle:visible');if(await theory.count())await page.waitForFunction(()=>[...document.querySelectorAll('#suiteTheoryContext #theoryArticle')].filter(el=>getComputedStyle(el).display!=='none').every(el=>el.getAttribute('aria-busy')!=='true'));await page.waitForTimeout(25);};const reset=async()=>{await page.goto(url);await settle();};const technical=async()=>{await page.getByRole('button',{name:/Research \/ Provenance|Research \/ Proveniență/i}).click();await page.locator('[data-product-surface="technical"]').waitFor();};
 const prepare=async()=>page.evaluate(()=>{document.querySelectorAll('[data-product-tab-audit]').forEach(e=>e.removeAttribute('data-product-tab-audit'));const q='a[href],button,input:not([type="hidden"]),select,textarea,summary,[tabindex],audio[controls],video[controls],[contenteditable="true"]';const visible=e=>{const s=getComputedStyle(e),r=e.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&r.width>0&&r.height>0;};const disabled=e=>e.hasAttribute('disabled')||e.getAttribute('aria-disabled')==='true';const blocked=e=>{if(e.closest('[aria-hidden="true"],[inert]'))return true;const closed=e.closest('details:not([open])');if(!closed)return false;const summary=closed.querySelector(':scope > summary');return !(summary&&(e===summary||summary.contains(e)));};const radios=e=>{if(!(e instanceof HTMLInputElement)||e.type!=='radio'||!e.name)return true;const group=[...document.querySelectorAll(`input[type="radio"][name="${CSS.escape(e.name)}"]`)].filter(x=>visible(x)&&!disabled(x)&&!blocked(x));const checked=group.find(x=>x.checked);return checked?checked===e:group[0]===e;};return [...document.querySelectorAll(q)].filter(e=>e.tabIndex>=0&&visible(e)&&!disabled(e)&&!blocked(e)&&radios(e)).map((e,i)=>{const id=String(i);e.setAttribute('data-product-tab-audit',id);return {id,tag:e.tagName,domId:e.id||'',text:(e.getAttribute('aria-label')||e.textContent||'').trim().replace(/\s+/g,' ').slice(0,80)};});});
 const traverse=async label=>{const expected=await prepare();assert(expected.length>0,`${label}: no sequential keyboard targets`);await page.evaluate(()=>{document.body.setAttribute('tabindex','-1');document.body.focus();});const seen=new Set();for(let i=0;i<expected.length+24&&seen.size<expected.length;i++){await page.keyboard.press('Tab');const id=await page.evaluate(()=>document.activeElement?.getAttribute('data-product-tab-audit')??null);if(id!==null)seen.add(id);}const missing=expected.filter(x=>!seen.has(x.id));assert.deepEqual(missing,[],`${label}: Tab did not reach ${JSON.stringify(missing)}`);await page.evaluate(()=>{document.body.removeAttribute('tabindex');document.querySelectorAll('[data-product-tab-audit]').forEach(e=>e.removeAttribute('data-product-tab-audit'));});};
 const visit=async(label,action)=>{await reset();if(action)await action();await settle();await traverse(label);};
 await visit('home');await visit('pathway',async()=>{await page.getByRole('button',{name:/How can repetition make a claim seem more true/i}).click();});await visit('mechanism-drawer',async()=>{await page.getByRole('button',{name:/How can repetition make a claim seem more true/i}).click();await page.locator('[data-suite-focus="familiarity"]').click();});await visit('interventions',async()=>{await page.getByRole('button',{name:'Interventions'}).click();});await visit('atlas-major',async()=>{await page.getByRole('button',{name:'Full Model Atlas'}).click();});await visit('atlas-expanded',async()=>{await page.getByRole('button',{name:'Full Model Atlas'}).click();await page.locator('#atlasFilter').selectOption('memory-familiarity');});await visit('theory',async()=>{await page.getByRole('button',{name:'Theory / Learn'}).click();await page.getByRole('button',{name:/Open full reader|Deschide reader-ul complet/i}).click();});await visit('technical',technical);
 for(const [name,selector] of [['planning','[data-suite-view="planning"]'],['reference','[data-suite-view="reference"]'],['search','[data-suite-tool="search"]'],['inspector','[data-suite-tool="inspector"]']])await visit(name,async()=>{await technical();await page.locator(selector).first().click();});
 // Verify collapsed pathway branches are keyboard-operable and reveal their mechanism controls only after opening.
 await reset();await page.getByRole('button',{name:/How can repetition make a claim seem more true/i}).click();const branch=page.locator('.secondary-branch').first();const summary=branch.locator(':scope > summary');await summary.focus();assert.equal(await branch.evaluate(el=>el.open),false);await page.keyboard.press('Space');assert.equal(await branch.evaluate(el=>el.open),true,'Space must open a secondary pathway branch');const child=branch.locator('button').first();await page.keyboard.press('Tab');assert.equal(await child.evaluate(el=>document.activeElement===el),true,'Tab must reach expanded secondary mechanism');
 assert.deepEqual(errors,[],'keyboard traversal audit must not introduce runtime/resource errors');console.log('Keyboard traversal regression passed for Product Concept surfaces and progressive pathway disclosure.');
}finally{if(browser)await browser.close();await new Promise(resolve=>server.close(resolve));}
